{
  "name": "plex-collection-shuffler",
  "version": "1.0.0",
  "description": "Play a random episode from a Plex collection on one or more Plex clients.",
  "license": "MIT",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "setup": "node src/setup.js",
    "start": "node src/index.js",
    "test": "node --test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "webhook": "node src/webhook.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "fast-xml-parser": "^5.3.2"
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
